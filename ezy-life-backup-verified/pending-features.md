# EZY Life — Pending Features (queued after v3 build)

1. **Save username/password on login** — "Remember me" checkbox, pre-fills credentials on return visit (localStorage)
2. **Google Calendar sync** — Link Google Calendar account so GCal events show up in the Time Blocking / Calendar view (OAuth + Google Calendar API)
3. **Goal categories — rename** — Ability to edit the name of existing goal categories (not just add/delete)
4. **Main screen — large draggable category blocks** — Trading / Life / Tools shown as large clickable blocks on a home dashboard, drag to reorder

---

## Completed (2026-07-18)

- **Trading/Life sidebar headers** — Larger, bolder box (3px border, uppercase, bigger font, glow effect)
- **Drag-and-drop transitions** — Cleaner animated line indicator, scale effects, glow highlights on drop targets
- **Goal category reorder** — Drag handle (⠿) on each category in the Edit Categories modal; drag to reorder
- **Emoji picker** — Wider (360px), taller grid, bigger emoji (22px); added "Type emoji" input row at top so you can use your native Android keyboard or paste any emoji directly
- **Data preservation** — Carlos never clears your inputted data when making changes
