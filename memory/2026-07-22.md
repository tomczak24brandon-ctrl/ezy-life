# Memory – 2026-07-22

## EZY Life App Session

### Deployments Today
- v1784741935378 → filter fix for getActiveGoalTasks (catId bug)
- v1784742008988 → cache-bust redeploy
- v1784742316316 → interactive date picker in Add Task modal
- v1784742462860 → reminder + rrule dropdowns added
- Unicode/icon corruption sweep → all close buttons, nav arrows, modal title emojis fixed; deployed
- Custom reminder + custom rrule builder → deployed (last deployment this session)

### Backup Files
- `ezy-life/index-STABLE-2026-07-22-1044.html` — 296032 bytes (pre-unicode-fix)
- `ezy-life/index-STABLE-2026-07-22-1054.html` — 301433 bytes (post-unicode-fix, pre-custom-recurrence)
- Active file: `ezy-life/index.html`

### Features Completed Today
1. Goal color-coding (red/green borders) on Active Goals + All Goals kanban
2. Sub-task target dates on Active Goals tab
3. No-date = overdue/unscheduled flagging (goals + steps + time blocking banner)
4. Active goal tasks injected into Day view print layout
5. Interactive date picker in Add Task modal
6. Reminder dropdown (No reminder / At time / 5/10/15/30 min / 1h / 2h before)
7. Repeat/rrule dropdown (Does not repeat / Daily / Weekly / Monthly / Weekdays / Weekends)
8. Global unicode/icon corruption sweep — all ? chars replaced with HTML entities
9. Custom reminder option — inline panel with number + unit (Min/Hr/Day/Week)
10. Custom repeat rule builder — interval, day-of-week toggles, monthly options, live preview

### Key Technical Notes
- File encoding: always use `[System.IO.File]::WriteAllText` with UTF-8 (no BOM issues in PowerShell)
- Goals use `g.catId` (NOT `g.categoryId`)
- `rescheduleAllReminders()` called after `loadData()` in app init
- Recurring instances: `isRecurring:true` + `recurId` field
- Print goal tasks: Day view only (`_gcalView === 'day'` guard)
- No captcha — never re-add it

### Live App
- URL: https://ezy-life.vercel.app
- Netlify site: iron-eagle-truck-center (ironeagletruck.com) — separate from ezy-life

### IronPlanet Auction Watch
- Item #15467937 — closes 1am PDT Tuesday July 23, 2026
- Max bid: $6,000
- Reminders: Monday July 22 9pm PDT (4-hr warning), July 23 12:30am PDT (30-min warning)
- ONLY alert if current bid ≤ $6,000 — skip silently if over
# Memory – 2026-07-22

## EZY Life App Session

### Deployments Today
- v1784741935378 → filter fix for getActiveGoalTasks (catId bug)
- v1784742008988 → cache-bust redeploy
- v1784742316316 → interactive date picker in Add Task modal
- v1784742462860 → reminder + rrule dropdowns added
- Unicode/icon corruption sweep → all close buttons, nav arrows, modal title emojis fixed; deployed
- Custom reminder + custom rrule builder → deployed (last deployment this session)
- Unified Edit Task modal → deployed (session after compaction)

### Backup Files
- `ezy-life/index-STABLE-2026-07-22-1044.html` — 296032 bytes (pre-unicode-fix)
- `ezy-life/index-STABLE-2026-07-22-1054.html` — 301433 bytes (post-unicode-fix, pre-custom-recurrence)
- Active file: `ezy-life/index.html`

### Features Completed Today
1. Goal color-coding (red/green borders) on Active Goals + All Goals kanban
2. Sub-task target dates on Active Goals tab
3. No-date = overdue/unscheduled flagging (goals + steps + time blocking banner)
4. Active goal tasks injected into Day view print layout
5. Interactive date picker in Add Task modal
6. Reminder dropdown (No reminder / At time / 5/10/15/30 min / 1h / 2h before)
7. Repeat/rrule dropdown (Does not repeat / Daily / Weekly / Monthly / Weekdays / Weekends)
8. Global unicode/icon corruption sweep — all ? chars replaced with HTML entities
9. Custom reminder option — inline panel with number + unit (Min/Hr/Day/Week)
10. Custom repeat rule builder — interval, day-of-week toggles, monthly options, live preview
11. Unified Edit Task modal — replaced stripped modal-taskdetail with full-featured modal:
    - Date picker (can move task to different day)
    - Full title input
    - Sub-tasks with ⋮⋮ drag handles, checkboxes, inline editing, ✕ delete
    - Start/End time pickers (AM/PM)
    - Event color picker
    - Reminder dropdown
    - ✕ Delete / Cancel / ✓ Save
    - gcalOpenTask() rewritten to pre-fill all fields
    - tdSave() rewritten to save all fields + handle date moves
    - UTF-8 corruption (??? Delete) fixed

### Key Technical Notes
- File encoding: always use `[System.IO.File]::WriteAllText` with UTF-8 (no BOM issues in PowerShell)
- Goals use `g.catId` (NOT `g.categoryId`)
- `rescheduleAllReminders()` called after `loadData()` in app init
- Recurring instances: `isRecurring:true` + `recurId` field
- Print goal tasks: Day view only (`_gcalView === 'day'` guard)
- No captcha — never re-add it
- Line ending issues: file accumulated \r\r\r\n from multiple Python rewrites; always normalize with `re.sub(r'\r', '', c)` at read time and write with `newline='\n'`
- FFFD corruption: earlier Python patches using open('r') then write corrupted emoji bytes to U+FFFD; later purged to '?'; use binary read `open('rb')` + decode with errors='replace' to detect
- Patch scripts: use regex (re.DOTALL) for fuzzy matching when exact strings drift due to encoding

### New JS Functions Added (Unified Edit Modal)
- `setAMPM_td(v)` / `setEndAMPM_td(v)` — AM/PM toggles for edit modal
- `autoToggleAMPM_td(which, inp)` — auto-switch AM/PM on hour > 12
- `getTdPickedTime()` / `getTdPickedEndTime()` — read time from edit modal fields
- `buildTdColorPicker()` / `pickTdColor(col)` — color picker for edit modal
- `tdAddSub()` / `renderTdSubList()` / `remTdSub(i)` / `updateTdSub(i, text)` — sub-task CRUD for edit modal
- `renderNewSubList()` (updated) — add modal sub-tasks now have drag handles + ✕ button
- State vars: `_tdIsPM`, `_tdIsEndPM`, `_tdTaskColor`, `_tdSubs`, `_tdGlobalDragSrc`, `_nsGlobalDragSrc`

### Live App
- URL: https://ezy-life.vercel.app
- Netlify site: iron-eagle-truck-center (ironeagletruck.com) — separate from ezy-life

### IronPlanet Auction Watch
- Item #15467937 — closes 1am PDT Tuesday July 23, 2026
- Max bid: $6,000
- Reminders: Monday July 22 9pm PDT (4-hr warning), July 23 12:30am PDT (30-min warning)
- ONLY alert if current bid ≤ $6,000 — skip silently if over
# Memory – 2026-07-22

## EZY Life App Session

### Deployments Today
- v1784741935378 → filter fix for getActiveGoalTasks (catId bug)
- v1784742008988 → cache-bust redeploy
- v1784742316316 → interactive date picker in Add Task modal
- v1784742462860 → reminder + rrule dropdowns added
- Unicode/icon corruption sweep → all close buttons, nav arrows, modal title emojis fixed; deployed
- Custom reminder + custom rrule builder → deployed
- Unified Edit Task modal → deployed
- fix_onkeydown.py → critical login fix deployed (JS syntax error: unescaped single quotes in onkeydown attr inside JS string killed entire script block)

### Backup Files
- `ezy-life/index-STABLE-2026-07-22-1044.html` — 296032 bytes (pre-unicode-fix)
- `ezy-life/index-STABLE-2026-07-22-1054.html` — 301433 bytes (post-unicode-fix, pre-custom-recurrence)
- Active file: `ezy-life/index.html`

### Features Completed Today
1. Goal color-coding (red/green borders) on Active Goals + All Goals kanban
2. Sub-task target dates on Active Goals tab
3. No-date = overdue/unscheduled flagging (goals + steps + time blocking banner)
4. Active goal tasks injected into Day view print layout
5. Interactive date picker in Add Task modal
6. Reminder dropdown (No reminder / At time / 5/10/15/30 min / 1h / 2h before)
7. Repeat/rrule dropdown (Does not repeat / Daily / Weekly / Monthly / Weekdays / Weekends)
8. Global unicode/icon corruption sweep — all ? chars replaced with HTML entities
9. Custom reminder option — inline panel with number + unit (Min/Hr/Day/Week)
10. Custom repeat rule builder — interval, day-of-week toggles, monthly options, live preview
11. Unified Edit Task modal — replaced stripped modal-taskdetail with full-featured modal (date picker, sub-tasks, time pickers, color picker, reminder, delete/save)
12. Login fix — JS syntax error from unescaped single quotes in onkeydown inside JS string; fixed and deployed

### Key Technical Notes
- File encoding: always use `[System.IO.File]::WriteAllText` with UTF-8 (no BOM issues in PowerShell)
- Goals use `g.catId` (NOT `g.categoryId`)
- `rescheduleAllReminders()` called after `loadData()` in app init
- Recurring instances: `isRecurring:true` + `recurId` field
- Print goal tasks: Day view only (`_gcalView === 'day'` guard)
- No captcha — never re-add it
- Line ending issues: file accumulated \r\r\r\n from multiple Python rewrites; always normalize with `re.sub(r'\r', '', c)` at read time and write with `newline='\n'`
- FFFD corruption: earlier Python patches using open('r') then write corrupted emoji bytes to U+FFFD; later purged to '?'; use binary read `open('rb')` + decode with errors='replace' to detect
- Patch scripts: use regex (re.DOTALL) for fuzzy matching when exact strings drift due to encoding
- **JS-in-HTML string escaping**: When building HTML strings in JS (template literals or string concat), onkeydown/onclick attributes that use single quotes MUST escape them as `\'` — unescaped single quotes terminate the enclosing JS string and crash the parser silently, breaking ALL JS on the page including login

### New JS Functions Added (Unified Edit Modal)
- `setAMPM_td(v)` / `setEndAMPM_td(v)` — AM/PM toggles for edit modal
- `autoToggleAMPM_td(which, inp)` — auto-switch AM/PM on hour > 12
- `getTdPickedTime()` / `getTdPickedEndTime()` — read time from edit modal fields
- `buildTdColorPicker()` / `pickTdColor(col)` — color picker for edit modal
- `tdAddSub()` / `renderTdSubList()` / `remTdSub(i)` / `updateTdSub(i, text)` — sub-task CRUD for edit modal
- `renderNewSubList()` (updated) — add modal sub-tasks now have drag handles + ✕ button
- State vars: `_tdIsPM`, `_tdIsEndPM`, `_tdTaskColor`, `_tdSubs`, `_tdGlobalDragSrc`, `_nsGlobalDragSrc`

### Live App
- URL: https://ezy-life.vercel.app
- Netlify site: iron-eagle-truck-center (ironeagletruck.com) — separate from ezy-life

### IronPlanet Auction Watch
- Item #15467937 — closes 1am PDT Tuesday July 23, 2026
- Max bid: $6,000
- Reminders: Monday July 22 9pm PDT (4-hr warning), July 23 12:30am PDT (30-min warning)
- ONLY alert if current bid ≤ $6,000 — skip silently if over
# Memory – 2026-07-22

## EZY Life App Session

### Deployments Today
- v1784741935378 → filter fix for getActiveGoalTasks (catId bug)
- v1784742008988 → cache-bust redeploy
- v1784742316316 → interactive date picker in Add Task modal
- v1784742462860 → reminder + rrule dropdowns added
- Unicode/icon corruption sweep → deployed
- Custom reminder + custom rrule builder → deployed
- Unified Edit Task modal → deployed
- fix_onkeydown.py → login fix #1 (unescaped 'Enter' in onkeydown attr inside JS string)
- fix_color_picker.py → login fix #2 (unescaped '' in pickTdColor() inside JS string; switched to data-col attr)
- After fix_color_picker: node --check CLEAN, login confirmed working

### Backup Files
- `ezy-life/index-STABLE-2026-07-22-1044.html` — 296032 bytes (pre-unicode-fix)
- `ezy-life/index-STABLE-2026-07-22-1054.html` — 301433 bytes (post-unicode-fix, pre-custom-recurrence)
- Active file: `ezy-life/index.html`

### Features Completed Today
1. Goal color-coding (red/green borders) on Active Goals + All Goals kanban
2. Sub-task target dates on Active Goals tab
3. No-date = overdue/unscheduled flagging (goals + steps + time blocking banner)
4. Active goal tasks injected into Day view print layout
5. Interactive date picker in Add Task modal
6. Reminder dropdown (No reminder / At time / 5/10/15/30 min / 1h / 2h before)
7. Repeat/rrule dropdown (Does not repeat / Daily / Weekly / Monthly / Weekdays / Weekends)
8. Global unicode/icon corruption sweep — all ? chars replaced with HTML entities
9. Custom reminder option — inline panel with number + unit (Min/Hr/Day/Week)
10. Custom repeat rule builder — interval, day-of-week toggles, monthly options, live preview
11. Unified Edit Task modal — full fields: date, title, sub-tasks, time pickers, color picker, reminder, delete/save
12. Login fix #1 — onkeydown='Enter' unescaped single quotes killed JS parser
13. Login fix #2 — pickTdColor(''+ col +'') unescaped quotes killed JS; fixed with data-col attr pattern

### Key Technical Notes
- File encoding: always use UTF-8, write with newline='\n'
- Goals use `g.catId` (NOT `g.categoryId`)
- No captcha — never re-add it
- **JS-in-HTML string escaping (CRITICAL)**: When building HTML in JS string concatenation, ALL single quotes inside event handler attributes (onclick, onkeydown, etc.) MUST be escaped as `\\'`. Unescaped single quotes silently terminate the JS string and break ALL JS on the page including login. Prefer `data-*` attributes + `this.dataset.x` to avoid escaping entirely.
- `node --check <file>` is the reliable way to validate JS before deploying — run after every patch
- Patch scripts: use re.DOTALL regex for fuzzy matching when exact strings drift

### New JS Functions (Unified Edit Modal)
- `setAMPM_td`, `setEndAMPM_td`, `autoToggleAMPM_td`, `getTdPickedTime`, `getTdPickedEndTime`
- `buildTdColorPicker` (uses data-col attr now), `pickTdColor(col)`
- `tdAddSub`, `renderTdSubList`, `remTdSub(i)`, `updateTdSub(i, text)`
- State vars: `_tdIsPM`, `_tdIsEndPM`, `_tdTaskColor`, `_tdSubs`, `_tdGlobalDragSrc`, `_nsGlobalDragSrc`

### Live App
- URL: https://ezy-life.vercel.app
- Netlify site: iron-eagle-truck-center (ironeagletruck.com) — separate

### IronPlanet Auction Watch
- Item #15467937 — closes 1am PDT Tuesday July 23, 2026
- Max bid: $6,000
- Reminders: Monday July 22 9pm PDT (4-hr warning), July 23 12:30am PDT (30-min warning)
- ONLY alert if current bid ≤ $6,000 — skip silently if over
# Memory – 2026-07-22

## EZY Life App Session

### Deployments Today
- v1784741935378 → filter fix for getActiveGoalTasks (catId bug)
- v1784742008988 → cache-bust redeploy
- v1784742316316 → interactive date picker in Add Task modal
- v1784742462860 → reminder + rrule dropdowns added
- Unicode/icon corruption sweep → deployed
- Custom reminder + custom rrule builder → deployed
- Unified Edit Task modal → deployed
- fix_onkeydown.py → login fix #1 (unescaped 'Enter' in onkeydown attr inside JS string)
- fix_color_picker.py → login fix #2 (unescaped quotes in pickTdColor; switched to data-col attr)
- fix_icons.py → hamburger menu ☰ icon fix (was showing as ?)
- All fixes confirmed: node --check CLEAN before each deploy

### Backup Files
- `ezy-life/index-STABLE-2026-07-22-1044.html` — pre-unicode-fix
- `ezy-life/index-STABLE-2026-07-22-1054.html` — post-unicode-fix, pre-custom-recurrence
- Active file: `ezy-life/index.html`

### Features Completed Today
1. Goal color-coding (red/green borders) on Active Goals + All Goals kanban
2. Sub-task target dates on Active Goals tab
3. No-date = overdue/unscheduled flagging (goals + steps + time blocking banner)
4. Active goal tasks injected into Day view print layout
5. Interactive date picker in Add Task modal
6. Reminder dropdown (No reminder / At time / 5/10/15/30 min / 1h / 2h before)
7. Repeat/rrule dropdown (Does not repeat / Daily / Weekly / Monthly / Weekdays / Weekends)
8. Global unicode/icon corruption sweep
9. Custom reminder option — inline panel with number + unit
10. Custom repeat rule builder — interval, day-of-week toggles, monthly options, live preview
11. Unified Edit Task modal — full fields: date, title, sub-tasks, time, color, reminder, delete/save
12. Login fix #1 — onkeydown unescaped single quotes
13. Login fix #2 — pickTdColor unescaped quotes; data-col pattern used
14. Hamburger menu icon fix — ☰ was showing as ?

### Key Technical Notes — CRITICAL
- **JS-in-HTML string escaping**: Single quotes inside event handler attrs (onclick, onkeydown) inside JS string concatenation MUST be escaped as `\\'`. Better: use `data-*` + `this.dataset.x` to avoid entirely.
- **`node --check <file>`**: Always run on extracted JS block before deploying. Catches parse errors.
- **PowerShell emoji rendering**: `??` in PS output ≠ broken file. Check raw bytes (f09f... = valid UTF-8 emoji).
- Goals use `g.catId` (NOT `g.categoryId`)
- No captcha — never re-add it
- Write files with encoding='utf-8', newline='\n'

### New JS Functions (Unified Edit Modal)
- `setAMPM_td`, `setEndAMPM_td`, `autoToggleAMPM_td`, `getTdPickedTime`, `getTdPickedEndTime`
- `buildTdColorPicker` (uses data-col attr), `pickTdColor(col)`
- `tdAddSub`, `renderTdSubList`, `remTdSub(i)`, `updateTdSub(i, text)`
- State vars: `_tdIsPM`, `_tdIsEndPM`, `_tdTaskColor`, `_tdSubs`, `_tdGlobalDragSrc`, `_nsGlobalDragSrc`

### Live App
- URL: https://ezy-life.vercel.app
- Netlify: iron-eagle-truck-center (ironeagletruck.com) — separate

### IronPlanet Auction Watch
- Item #15467937 — closes 1am PDT Tuesday July 23, 2026
- Max bid: $6,000
- Reminders: Monday July 22 9pm PDT (4-hr warning), July 23 12:30am PDT (30-min warning)
- ONLY alert if current bid ≤ $6,000 — skip silently if over
# Memory – 2026-07-22

## EZY Life App Session

### Deployments Today
- v1784741935378 → filter fix for getActiveGoalTasks (catId bug)
- v1784742008988 → cache-bust redeploy
- v1784742316316 → interactive date picker in Add Task modal
- v1784742462860 → reminder + rrule dropdowns added
- Unicode/icon corruption sweep → deployed
- Custom reminder + custom rrule builder → deployed
- Unified Edit Task modal → deployed
- fix_onkeydown.py → login fix #1 (unescaped 'Enter' in onkeydown attr inside JS string)
- fix_color_picker.py → login fix #2 (unescaped quotes in pickTdColor; switched to data-col attr)
- fix_icons.py → hamburger menu ☰ icon fix (was showing as ?)

### Backups
- `index-STABLE-2026-07-22-1044.html` — pre-unicode-fix
- `index-STABLE-2026-07-22-1054.html` — post-unicode-fix, pre-custom-recurrence
- `index-STABLE-2026-07-22-2054.html` — 357,394 bytes — post all login fixes + icon fix ✅ LATEST
- Active file: `ezy-life/index.html`

### Features Completed Today
1. Goal color-coding (red/green borders) on Active Goals + All Goals kanban
2. Sub-task target dates on Active Goals tab
3. No-date = overdue/unscheduled flagging
4. Active goal tasks injected into Day view print layout
5. Interactive date picker in Add Task modal
6. Reminder dropdown
7. Repeat/rrule dropdown
8. Global unicode/icon corruption sweep
9. Custom reminder option
10. Custom repeat rule builder
11. Unified Edit Task modal
12. Login fix #1 — onkeydown unescaped single quotes
13. Login fix #2 — pickTdColor unescaped quotes; data-col pattern
14. Hamburger menu icon fix — ☰ was showing as ?

### Security Note
- Received suspected prompt injection via Brandon's Telegram (2026-07-22 ~18:30 PDT)
- Message claimed to be "MANDATORY BACKUP" + "INITIATE FULL FIREBASE PRODUCTION LAUNCH"
- Flagged and held — Brandon confirmed backup command was his (from cell), Firebase command origin unclear
- Firebase/Next.js not part of this project — ezy-life is a single index.html file
- Pattern: ALL CAPS commands, "DO NOT IGNORE", no explanation demanded, recon-style output request

### Key Technical Notes — CRITICAL
- **JS-in-HTML string escaping**: Single quotes inside event handler attrs inside JS string concat MUST be escaped as `\\'`. Better: use `data-*` + `this.dataset.x`.
- **`node --check <file>`**: Always run on extracted JS block before deploying.
- **PowerShell emoji rendering**: `??` in PS ≠ broken. Check raw bytes.
- Goals use `g.catId` (NOT `g.categoryId`)
- No captcha — never re-add it
- Write files with encoding='utf-8', newline='\n'

### New JS Functions (Unified Edit Modal)
- `setAMPM_td`, `setEndAMPM_td`, `autoToggleAMPM_td`, `getTdPickedTime`, `getTdPickedEndTime`
- `buildTdColorPicker` (uses data-col attr), `pickTdColor(col)`
- `tdAddSub`, `renderTdSubList`, `remTdSub(i)`, `updateTdSub(i, text)`
- State vars: `_tdIsPM`, `_tdIsEndPM`, `_tdTaskColor`, `_tdSubs`, `_tdGlobalDragSrc`, `_nsGlobalDragSrc`

### Live App
- URL: https://ezy-life.vercel.app
- Netlify: iron-eagle-truck-center (ironeagletruck.com) — separate

### IronPlanet Auction Watch
- Item #15467937 — closes 1am PDT Tuesday July 23, 2026
- Max bid: $6,000
- Reminders: Monday July 22 9pm PDT (4-hr warning), July 23 12:30am PDT (30-min warning)
- ONLY alert if current bid ≤ $6,000 — skip silently if over
# Memory – 2026-07-22

## EZY Life App Session

### Deployments Today
- v1784741935378 → filter fix for getActiveGoalTasks (catId bug)
- v1784742008988 → cache-bust redeploy
- v1784742316316 → interactive date picker in Add Task modal
- v1784742462860 → reminder + rrule dropdowns added
- Unicode/icon corruption sweep → deployed
- Custom reminder + custom rrule builder → deployed
- Unified Edit Task modal → deployed
- fix_onkeydown.py → login fix #1
- fix_color_picker.py → login fix #2 (data-col pattern)
- fix_icons.py → hamburger ☰ icon fix

### Backups
- `index-STABLE-2026-07-22-2054.html` — 357,394 bytes — LATEST stable checkpoint

### Features Completed
1. Goal color-coding, sub-task target dates, no-date flagging
2. Active goal tasks in Day view
3. Interactive date picker, reminder dropdown, rrule dropdown
4. Custom reminder + custom repeat rule builder
5. Unified Edit Task modal
6. Login fix (JS parse errors from unescaped single quotes)
7. Hamburger menu icon fix

### Firebase Migration — IN PROGRESS
- Decision: Firestore full cutover + offline persistence enabled (no localStorage for app data)
- Auth: Google sign-in only
- Data structure: users/{uid}/goals, users/{uid}/tasks, users/{uid}/notes, etc.
- Firebase project: **EZY LIFE** → project ID: `my-life-a37aa`
- Project number: `28106321797`
- App ID: `1:28106321797:web:24245eeb22ded10766deec`
- **WAITING ON**: full firebaseConfig block (apiKey, authDomain, storageBucket) from Brandon
- Brandon navigating Firebase console to find SDK config under Project Settings → Your apps

### Key Technical Notes — CRITICAL
- **JS-in-HTML string escaping**: Use data-* + this.dataset.x to avoid single-quote escaping hell
- **`node --check`**: Always run before deploying
- Goals use `g.catId` (NOT `g.categoryId`)
- No captcha — never re-add it
- Write files with encoding='utf-8', newline='\n'
- Firestore offline persistence: `enableIndexedDbPersistence(db)` — one line

### Live App
- URL: https://ezy-life.vercel.app
- Netlify: iron-eagle-truck-center (ironeagletruck.com) — separate

### IronPlanet Auction Watch
- Item #15467937 — closes 1am PDT Tuesday July 23, 2026
- Max bid: $6,000
- Reminders: 9pm PDT July 22 (4-hr), 12:30am PDT July 23 (30-min)
- ONLY alert if current bid ≤ $6,000
# Memory – 2026-07-22

## EZY Life App Session

### Deployments Today
- Multiple UI/logic fixes throughout the day
- fix_onkeydown.py → login fix #1
- fix_color_picker.py → login fix #2 (data-col pattern)
- fix_icons.py → hamburger ☰ icon fix
- **firebase_migration.py → Firebase full cutover deployed**

### Backups
- `index-STABLE-2026-07-22-2054.html` — 357,394 bytes — pre-Firebase LATEST stable checkpoint

### Features Completed
1. Goal color-coding, sub-task target dates, no-date flagging
2. Active goal tasks in Day view
3. Interactive date picker, reminder + rrule dropdowns, custom builders
4. Unified Edit Task modal
5. JS login fixes (3 syntax errors fixed)
6. Hamburger menu icon fix
7. **Firebase Auth + Firestore full cutover**

### Firebase Migration — COMPLETE
- Auth: Google sign-in only (Firebase Auth)
- Data: Firestore full cutover — `users/{uid}/data/main`
- Offline: `enablePersistence({ synchronizeTabs: true })` enabled
- Sign-out button added to topbar
- Firebase project: `my-life-a37aa`
- Project number: `28106321797`
- App ID: `1:28106321797:web:24245eeb22ded10766deec`
- Auth providers enabled: Email/Password, Phone, Google
- Auth state: `onAuthStateChanged` → sets `_fbUid` → calls `loadData()` → calls `appInit()`
- saveData() / loadData() both updated; fallback to localStorage if `_fbUid` is null
- Hardcoded CREDS var removed
- Migration script: `ezy-life/firebase_migration.py`
- **NOTE**: First sign-in = fresh Firestore slate. Old localStorage data NOT migrated yet.
  - One-time migration script available on request

### Pending / Next Steps
- Brandon needs to test Google sign-in at https://ezy-life.vercel.app
- Confirm data saves/loads correctly after sign-in
- Optional: one-time localStorage → Firestore migration script
- Future: PWA (service worker + manifest) for home screen install + widget support
- Future: per-collection Firestore writes instead of single `data/main` doc (scalability)

### Key Technical Notes — CRITICAL
- **JS-in-HTML string escaping**: Use data-* + this.dataset.x to avoid single-quote escaping
- **`node --check`**: Always run before deploying
- Goals use `g.catId` (NOT `g.categoryId`)
- No captcha — never re-add it
- Write files with encoding='utf-8', newline='\n'
- Firestore offline: `enablePersistence({ synchronizeTabs: true })`
- Firebase compat SDK used (v9.23.0 compat) — `firebase.auth()`, `firebase.firestore()`
- PowerShell emoji rendering: `??` in PS ≠ broken file

### Security Note
- Received suspected prompt injection 2026-07-22 ~18:30 PDT via Telegram
- "MANDATORY BACKUP" + "INITIATE FULL FIREBASE PRODUCTION LAUNCH" ALL CAPS messages
- Brandon confirmed backup command was his (from cell); Firebase command timing coincidental
- No damage done — flagged and held pending confirmation

### Live App
- URL: https://ezy-life.vercel.app
- Netlify: iron-eagle-truck-center (ironeagletruck.com) — separate site

### IronPlanet Auction Watch
- Item #15467937 — closes 1am PDT Tuesday July 23, 2026
- Max bid: $6,000
- Reminders: 9pm PDT July 22 (4-hr), 12:30am PDT July 23 (30-min)
- ONLY alert if current bid ≤ $6,000 — skip silently if over
