# Memory — 2026-07-21

## EZY Life App — Session Work

### State at compaction (10:54 AM PDT)
- Production URL: https://ezy-life.vercel.app
- Source: `C:\Users\BIG D\.openclaw\workspace\ezy-life\index.html`
- **UTF-8 encoding is clean** — zero `??` corruption as of last deploy
- **Daily Review popup** — fully removed
- **Goals tab bar** — global display bug fixed (CSS scoped to active page only)

### Stable backups on disk (in ezy-life/)
- `index-STABLE-July21.html` — 274,158 bytes — created 10:42:56 AM
- `index-STABLE-2026-07-21-1053.html` — 276,290 bytes — created 10:49:57 AM (post-checkbox update)
- `index.backup-2026-07-21-0856.html` — CORRUPTED (UTF-8 mangled, do not use)
- `ezy-life-backup-verified/` folder — CORRUPTED (do not use)

### Features deployed today (in order)
1. Goals tab bar fix — CSS scoped so tab bar only shows inside Goals page
2. Daily Review modal + JS + CSS + routing — fully removed
3. All emojis restored — 130 corrupted `??` lines fixed via Node.js script
4. Active Goals tab filter — shows only first active goal per category
5. Active Goals task filter — shows first incomplete task (auto-advance)
6. Interactive checkbox on card face — marks task complete, recalculates progress, auto-advances to next incomplete task
7. Archiving fires correctly when last task checked (goal moves to Complete tab)
8. Empty state for goals with no tasks: shows `+ Add steps`
9. Task font size increased to 1.05rem on dashboard cards

### Key technical notes
- **NEVER use PowerShell Get-Content/Set-Content or Invoke-WebRequest to write HTML** — corrupts multi-byte UTF-8 (emojis → `??`)
- Safe write methods: Node.js `fs.writeFileSync(path, str, 'utf8')` or `[System.IO.File]::WriteAllBytes()`
- Vercel auth token `nfp_z8…dd41` returns forbidden/invalidToken — do not use for API calls
- Deploy command: `vercel --prod --yes` from `ezy-life/` directory
- Rollback: `vercel promote <deployment-url> --scope iron-eagle-truck-center --yes`
- Last known-good deployment before today's session: `ezy-life-80yzj8fkf-iron-eagle-truck-center.vercel.app`

### Functions added/modified today
- `renderGoalsDashboard()` — rebuilt with per-category first-goal filter, first-incomplete-task display, checkbox, empty state
- `gdDashToggleStep(goalId, stepIdx)` — new function; toggles step, recalculates progress, fires archiving if 100%, re-renders dashboard
- `gdToggleStep()` — unchanged (modal detail logic)

### Pending / not done
- No pending tasks at compaction time
- Brandon may continue UI changes after this memory flush
# Memory — 2026-07-21

## EZY Life App — Session Work

### State at compaction (10:54 AM PDT)
- Production URL: https://ezy-life.vercel.app
- Source: `C:\Users\BIG D\.openclaw\workspace\ezy-life\index.html`
- **UTF-8 encoding is clean** — zero `??` corruption as of last deploy
- **Daily Review popup** — fully removed
- **Goals tab bar** — global display bug fixed (CSS scoped to active page only)

### Stable backups on disk (in ezy-life/)
- `index-STABLE-July21.html` — 274,158 bytes — created 10:42:56 AM
- `index-STABLE-2026-07-21-1053.html` — 276,290 bytes — created 10:49:57 AM (post-checkbox update)
- `index-STABLE-2026-07-21-1202.html` — 286,039 bytes — created 12:00:29 PM (post time-rollover fix)
- `index-STABLE-2026-07-21-1730.html` — 286,019 bytes — created 5:24:48 PM (post subtitle removal)
- `index.backup-2026-07-21-0856.html` — CORRUPTED (UTF-8 mangled, do not use)
- `ezy-life-backup-verified/` folder — CORRUPTED (do not use)

### Features deployed today (in order)
1. Goals tab bar fix — CSS scoped so tab bar only shows inside Goals page
2. Daily Review modal + JS + CSS + routing — fully removed
3. All emojis restored — 130 corrupted `??` lines fixed via Node.js script
4. Active Goals tab filter — shows only first active goal per category
5. Active Goals task filter — shows first incomplete task (auto-advance)
6. Interactive checkbox on card face — marks task complete, recalculates progress, auto-advances to next incomplete task
7. Archiving fires correctly when last task checked (goal moves to Complete tab)
8. Empty state for goals with no tasks: shows `+ Add steps`
9. Task font size increased to 1.05rem on dashboard cards
10. Time input rollover bug fixed — direction-aware AM/PM crossing at 11↔12 boundary only
11. Fix-time-up.js written — cleaner symmetric rollover logic (up-arrow stuck at 12 PM resolved)
12. "Google Calendar view" subtitle removed from Time Blocking page header
13. Time Blocking default view changed from Week → Day
14. "Sign In ?" button corruption fixed → clean "Sign In"
15. Goals tab edit changed from single pipe-delimited prompt → 3 sequential prompts
16. Goal-level overdue notifications added to calendar banner (orange row for overdue goals, red for overdue sub-tasks)
17. `getOverdueGoals()` function added — checks active goals past targetDate, not 100% complete

### Key technical notes
- **NEVER use PowerShell Get-Content/Set-Content or Invoke-WebRequest to write HTML** — corrupts multi-byte UTF-8 (emojis → `??`)
- Safe write methods: Node.js `fs.writeFileSync(path, str, 'utf8')` or `[System.IO.File]::WriteAllBytes()`
- Vercel auth token `nfp_z8…dd41` returns forbidden/invalidToken — do not use for API calls
- Deploy command: `vercel --prod --yes` from `ezy-life/` directory
- Rollback: `vercel promote <deployment-url> --scope iron-eagle-truck-center --yes`
- Last known-good deployment before today's session: `ezy-life-80yzj8fkf-iron-eagle-truck-center.vercel.app`
- **fix-time-up.js** — AM/PM flips ONLY at 11↔12 boundary (up: prev=11,hr=12 or prev=12,hr=1; down: prev=12,hr=11 or prev=1,hr=12)

### Functions added/modified today
- `renderGoalsDashboard()` — rebuilt with per-category first-goal filter, first-incomplete-task display, checkbox, empty state
- `gdDashToggleStep(goalId, stepIdx)` — new function; toggles step, recalculates progress, fires archiving if 100%, re-renders dashboard
- `gdToggleStep()` — unchanged (modal detail logic)
- `autoToggleAMPM()` — rewritten with clean direction-aware logic; data-prev tracking
- `getOverdueGoals()` — new function; scans active goals for past targetDate
- `renderGCal()` banner — expanded to two rows: orange (goals) + red (sub-tasks)

### Pending / not done
- No pending tasks at end of session

## Evening Session (9:31–9:39 PM PDT)

### Features deployed
18. `getOverdueGoals()` added — scans active goals for past `targetDate`, not 100% complete
19. Calendar overdue banner expanded to two rows: orange for overdue goals, red for overdue sub-tasks
20. Time Blocking layout fix — Goals board was bleeding in due to CSS isolation gap
    - Root cause: `main-content` hidden via inline JS style only (could be overridden)
    - Fix: Added `.main.tb-mode > #main-content { display:none !important }` CSS class
    - Both `showPage()` and `_showPageInternal()` now toggle `.tb-mode` class on `.main`
    - Double-lock: inline style + CSS class with `!important`
    - Overdue banner preserved and confirmed intact

### Stable backup as of ~9pm
- `index-STABLE-2026-07-21-1730.html` — last known clean backup before evening session
- No new backup created during evening session (changes were additive/safe)

### Pending
- No pending tasks at end of session

## Late Evening (9:38–9:41 PM PDT)

### Features deployed
21. Overdue banner made clickable on Time Blocking page
    - Added `goalsShowOverdue()` function
    - Click navigates to Goals page, All Goals tab (index 1)
    - Scrolls to first overdue goal card, flashes red pulse animation (3× over 2s)
    - "View Goals →" hint shown on right side of banner
    - `cursor:pointer` + tooltip added
    - Overdue calculation logic and banner styling untouched

### No pending tasks at end of session
